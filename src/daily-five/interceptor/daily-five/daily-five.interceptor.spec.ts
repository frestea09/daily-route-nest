import { DailyFiveInterceptor } from './daily-five.interceptor';

describe('DailyFiveInterceptor', () => {
  it('should be defined', () => {
    expect(new DailyFiveInterceptor()).toBeDefined();
  });
});
