import { DailyFiveMiddleware } from './daily-five.middleware';

describe('DailyFiveMiddleware', () => {
  it('should be defined', () => {
    expect(new DailyFiveMiddleware()).toBeDefined();
  });
});
